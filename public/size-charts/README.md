# Size charts

Drop your size-chart images here (PNG/JPG). They appear automatically on the
/size-guide page, in alphabetical order. Name them descriptively, e.g.:

- jersey-youth.png
- jersey-adult.png
- pants-size-chart.png
- hat-sizing.png

The filename (minus extension, dashes → spaces) is shown as the chart's title.
